@model IEnumerable<Progetto_MVC_CORE_ENTITY.Models.TipoServizio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoServizio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoServizio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoServizio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoServizio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoServizio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
